apiVersion: v1
kind: Pod
metadata:
  name: dnsutils
  namespace: default
spec:
  containers:
  - name: dnsutils
    image: praqma/network-multitool:f0541c8
    # command:
    #   - sleep
    #   - "3600"
    imagePullPolicy: IfNotPresent
  restartPolicy: Always
